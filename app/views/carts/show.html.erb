<h1>Your cart</h1>
<% @cart.line_items.each do |li| %>
  <p>
    <%= li.product.title %> — <%= li.quantity %> x <%= number_to_currency(li.product.price) %>
    <%= button_to "Remove", cart_remove_item_path(li), method: :delete %>
  </p>
<% end %>
<p>Total: <%= number_to_currency(@cart.total) %></p>
<%= link_to "Checkout", new_order_path %>
